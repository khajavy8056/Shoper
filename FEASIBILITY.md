# امکان‌سنجی فنی: دسترسی به داده‌های ترب و ورود به ووکامرس

> پاسخ دقیق به این نگرانی: «سایت‌ها جلوی این ارتباطات را می‌گیرند؛ آیا اصلاً می‌شود یا نه؟ و چطور باید کار کنیم؟»
> تاریخ: ۱۴۰۵/۰۵/۲۴ — پروژه‌ی Shoper

---

## ۱. نتیجه‌گیری صریح (بله، می‌شود — با یک شرط مهم)

**بله، این کار شدنی است**، اما باید سه واقعیت را از همان ابتدا بپذیریم و معماری را بر اساس آن بسازیم:

1. **API داخلی ترب به‌طور عمومی و بدون کلید کار می‌کند** — این را چندین پروژه‌ی فعال در گیت‌هاب و PyPI اثبات کرده‌اند (جزئیات در بخش ۲).
2. **محیط سندباکس فعلی به اینترنت ایران دسترسی ندارد** و این دلیل خطاهای اتصال ماست، نه اینکه ترب ما را بلاک کرده باشد (اثبات در بخش ۳).
3. **افزونه باید روی سرور وردپرسی اجرا شود که به ترب دسترسی دارد** (هاست ایران یا سرور ایران). روی هاست‌های خارجی ممکن است اتصال برقرار نشود یا به‌خاطر تحریم/مسدودسازی با مشکل مواجه شود.

برای مقاوم‌سازی، افزونه را با **چند استراتژی پشتیبان (fallback)** می‌سازیم تا اگر یک روش کار نکرد، روش بعدی به‌طور خودکار امتحان شود (جزئیات در بخش ۶).

---

## ۲. آیا API ترب واقعاً بدون کلید/لاگین کار می‌کند؟ (بله)

شواهد از پروژه‌های واقعی که همین الان از این API استفاده می‌کنند:

### ۲.۱ پکیج رسمی PyPI: `torob-integration`
- آدرس: https://pypi.org/project/torob-integration/
- مخزن: https://github.com/hamidrezafarzin/Torob-Integration (۳۱ ستاره)
- کد واقعی اتصال (بدون هیچ هدر، کوکی یا کلید خاص):
  ```python
  self._search_url = "https://api.torob.com/v4/base-product/search/"
  response = requests.get(url, params, timeout=5)
  result_json = response.json()
  ```
- یعنی یک `GET` ساده با `requests.get` پاسخ JSON می‌گیرد.

### ۲.۲ اسکریپت `torob-scraper`
- آدرس: https://github.com/mahdi-marjani/torob-scraper/blob/main/product_scraper.py
- کد:
  ```python
  response = requests.get(page_url)
  results = response.json()["results"]
  ```
- صفحه‌بندی را هم با فیلد `next` به‌سادگی دنبال می‌کند.

### ۲.۳ پروژه‌ی `torob-bot`
- آدرس: https://github.com/omidima/torob-bot
- توضیح رسمی نویسنده: «ترب یک public api resource دارد که می‌توان از طریق دامنه‌ی api.torob.ir استفاده کرد.»
- این پروژه مشخصات فنی (`structural_specs`)، variants و لیست فروشندگان را با موفقیت استخراج می‌کند.

### ۲.۴ سرویس‌های شخص‌ثالث تجاری
- [MajidAPI](https://majidapi.ir/doc/ترب) و [Parse.bot](https://parse.bot/marketplace/d925cf61-d99f-41ad-9765-d68fc7fcfac1/torob-com-api) همین API را به‌صورت سرویس می‌فروشند — یعنی API به‌اندازه‌ای پایدار هست که کسب‌وکار روی آن بنا شده.

**نکته‌ی فنی درباره‌ی anti-bot:** برخلاف سایت‌هایی مثل دیجی‌کالا که روی HTML خود ضداسکرپ سنگین دارند، ترب **وب‌اپلیکیشن است و داده‌هایش را از یک API جدا (api.torob.com) به‌صورت JSON می‌گیرد**. این اندپوینت‌ها برای مصرف فرانت‌اند خود سایت طراحی شده‌اند و معمولاً محدودیت کمتری نسبت به صفحات HTML دارند. هدرهای مرورگر و `Referer: https://torob.com/` کافی است.

---

## ۳. چرا در سندباکس ما اتصال برقرار نشد؟ (اثبات اینکه ایراد از ماست نه ترب)

تست‌های شبکه را با دقت تحلیل کردم:

| مقصد | نتیجه از سندباکس |
|------|------------------|
| `github.com` | ✅ HTTP 200 (کار می‌کند) |
| `google.com` | ❌ SSL_ERROR_SYSCALL |
| `wordpress.org` | ❌ SSL_ERROR_SYSCALL |
| `www.digikala.com` | ❌ SSL_ERROR_SYSCALL |
| `api.torob.com` | ❌ SSL_ERROR_SYSCALL |
| `torob.ir` و `api.torob.ir` | ❌ SSL_ERROR_SYSCALL |
| پورت ۸۰ (HTTP بدون TLS) ترب | ❌ Empty reply from server |

**تحلیل:**
- اگر مشکل «بلاک شدن توسط ترب» بود، بایستی google.com و wordpress.org کار می‌کردند (که نکردند).
- `github.com` که در **لیست سفید (allowlist)** سندباکس است کار می‌کند، اما بقیه‌ی سایت‌ها در لایه‌ی شبکه بسته می‌شوند.
- در تست `openssl s_client` به ترب، **صفر بایت گواهی از سرور دریافت شد** و اتصال بلافاصله بسته شد — این رفتار مشخصه‌ی یک **فایروال/پراکسی خروجی سندباکس** است، نه پاسخ واقعی سرور ترب.

**نتیجه:** شکست اتصال در سندباکس به‌هیچ‌وجه نشان‌دهنده‌ی غیرقابل‌دسترس بودن ترب نیست. این صرفاً محدودیت محیط توسعه‌ی ماست.

---

## ۴. آیا ترب از Cloudflare یا WAF سنگین استفاده می‌کند؟

- ترب یک سرویس ایرانی است و معماری‌اش مبتنی بر زیرساخت ایران است (در گذشته `arvancloud.ir` دیده شده).
- **APIهای ج‌سان ترب روی دامنه‌ی `api.torob.com` همان اندپوینت‌هایی هستند که وب‌سایت خودش استفاده می‌کند** و شواهد پروژه‌های فعال نشان می‌دهد با درخواست ساده‌ی HTTP پاسخ می‌دهند.
- خطر واقعی نه در فاز اول (یک کاربر یک محصول می‌سازد) که در **ارسال درخواست‌های انبوه و پرتکرار** است که می‌تواند به محدودیت نرخ (rate limit) یا مسدودسازی IP منجر شود.
- چون سناریوی ما **ساخت یک محصول در هر بار** توسط کاربر انسانی است (نه خزش انبوه)، ریسک بسیار پایین است.

---

## ۵. مکانیزم دقیق اتصال (چطور به ترب وصل شویم)

در افزونه، درخواست‌ها با **WordPress HTTP API** (نه cURL خام) ارسال می‌شوند تا با تمام تنظیمات هاست سازگار باشند:

```php
$response = wp_remote_get($api_url, array(
    'timeout'     => 20,
    'redirection' => 3,
    'user-agent'  => 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 '
                     .'(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
    'headers'     => array(
        'Accept'          => 'application/json, text/plain, */*',
        'Accept-Language' => 'fa-IR,fa;q=0.9,en;q=0.8',
        'Referer'         => 'https://torob.com/',
        'Origin'          => 'https://torob.com',
    ),
    'sslverify'   => true,   // روی هاست ایران معتبر؛ در صورت لزوم قابل تنظیم
));
```

### جریان دو مرحله‌ای برای گرفتن مشخصات کامل:

```
مرحله ۱ (جستجو با نام):
  GET https://api.torob.com/v4/base-product/search/?q={نام محصول}&page=0&size=10&source=next_desktop
  → results[].more_info_url که شامل prk و search_id است.

مرحله ۲ (جزئیات کامل):
  GET https://api.torob.com/v4/base-product/details/?prk={prk}&search_id={search_id}&source=next_desktop
  → name1, name2, image_url, variants, structural_specs, key_specs, products_info
```

برای **ورود با لینک مستقیم محصول** (مثلاً `torob.com/p/abc123/.../`):
- شناسه‌ی محصول از URL استخراج می‌شود؛
- چون endpoint جزئیات به `search_id` هم نیاز دارد، یک فراخوانی کوتاه جستجو/پیشنهاد برای گرفتن `search_id` انجام می‌شود.

---

## ۶. استراتژی مقاوم‌سازی (مهم‌ترین بخش)

برای اینکه «اگر این روش کار نکرد» کل چیز از کار نیفتد، یک **لایه‌ی انتزاعی منبع داده (Data Source Driver)** می‌سازیم با چند راننده به ترتیب اولویت:

1. **راننده‌ی مستقیم (Direct API):** اتصال مستقیم به `api.torob.com` — روش اصلی و رایگان.
2. **راننده‌ی HTML fallback:** اگر JSON API در دسترس نبود، دانلود صفحه‌ی HTML ترب و استخراج داده‌ی درون تگ `<script>` (نشانه‌گذاری `__NUXT`/JSON) که همان داده‌ی API است.
3. **راننده‌ی شخص‌ثالث (اختیاری):** اتصال به MajidAPI یا Parse.bot با کلید API کاربر (در تنظیمات افزونه). این برای هاست‌هایی است که آی‌پی‌شان به هر دلیلی به ترب مستقیم نمی‌رسد.

علاوه بر این:

- **کش (Transient):** پاسخ جستجو و جزئیات برای چند ساعت در وردپرس کش می‌شود تا هم درخواست تکراری به ترب نرود و هم اگر اتصال موقتاً قطع شد، داده‌ی قبلی نمایش داده شود.
- **تاخیر و backoff:** در صورت خطای 429/403، به‌صورت خودکار بعد از چند ثانیه با تاخیر نمایی دوباره تلاش می‌شود.
- **چند User-Agent:** چرخش بین چند User-Agent مرورگر واقعی.
- **دکمه‌ی «تست اتصال» در صفحه‌ی تنظیمات:** قبل از استفاده، کاربر می‌تواند یک‌بار اتصال به ترب را از روی سرور خودش تست کند تا مطمئن شویم هاستش دسترسی دارد.
- **گزارش خطای شفاف:** اگر هیچ‌کدام از روش‌ها جواب نداد، پیام دقیق به مدیر نشان داده می‌شود (مثلاً «هاست شما به ترب دسترسی ندارد؛ از یک سرویس واسط یا هاست ایران استفاده کنید»).

---

## ۷. ریسک‌های واقعی و راه‌حل آن‌ها

| ریسک | احتمال | توضیح | راه‌حل |
|------|--------|--------|--------|
| API ترب در آینده تغییر کند | متوسط | API رسمی نیست و ممکن است فیلدها عوض شوند | لایه‌ی مپر (mapper) جداگانه + راننده‌های چندگانه + قابلیت به‌روزرسانی سریع |
| هاست کاربر به ترب دسترسی نداشته باشد (هاست خارج/تحریم) | متوسط | برخی هاست‌های خارجی آی‌پی ایران را بلاک می‌کنند یا برعکس | دکمه‌ی تست اتصال + پشتیبانی از پروکسی/سرویس شخص‌ثالث در تنظیمات |
| محدودیت نرخ با استفاده‌ی مکرر | کم | سناریوی ما تک‌محصولی است | کش + تاخیر بین درخواست‌ها |
| دانلود تصویر از `image.torob.com` با خطا مواجه شود | کم | hotlink protection احتمالی | دانلود با هدر Referer و User-Agent؛ در صورت شکست، درج لینک خارجی به‌عنوان fallback و اطلاع به کاربر |
| کاراکترهای فارسی در نام ویژگی | قطعی | ووکامرس برای taxonomy نیاز به slug لاتین دارد | نقشه‌ی slug دستی برای ویژگی‌های رایج (پردازنده→processor) + sanitize هوشمند |
| تغییر IP ترب / دامنه‌ی `.ir` در مقابل `.com` | کم | هر دو دامنه پشتیبانی می‌شوند | استفاده از دامنه‌ی قابل تنظیم؛ به‌طور پیش‌فرض `.com` |
| کپی‌رایت تصاویر و توضیحات | — | داده متعلق به فروشندگان/ترب است | درج هشدار در افزونه؛ مسئولیت استفاده با کاربر |

---

## ۸. ثابت کردن عملی موضوع (چرا باید یک تست واقعی روی هاست بزنیم)

چون در سندباکس به اینترنت دسترسی نداریم، برای اطمینان ۱۰۰٪ پیشنهاد می‌کنم **قبل از تکمیل توسعه، یک تست اتصال واقعی** انجام شود به یکی از این دو روش:

1. **روی هاست/وردپرس واقعی شما:** یک اسکریپت کوچک PHP می‌نویسم که فقط همان endpoint جستجو را صدا بزند و نتیجه را نشان دهد. روی هاست ایران که اجرا شود، ظرف چند ثانیه قطعی بودن ارتباط تأیید می‌شود.
2. **اینجا در سندباکس با داده‌ی نمونه (mock):** ما توسعه را با یک فایل JSON نمونه (ضبط‌شده از پاسخ واقعی ترب) پیش می‌بریم تا کل UI و منطق ووکامرس را کامل بسازیم و آزمایش کنیم. سپس در لحظه‌ی نصب روی هاست واقعی، فقط کلید اتصال را می‌زنیم.

**این دو روش مکمل هم هستند:**
- Mock data → توسعه‌ی کامل UI و منطق بدون نیاز به شبکه.
- تست روی هاست واقعی → تأیید نهایی اتصال.

---

## ۹. جمع‌بندی و پاسخ به سؤال اصلی

> «چطور می‌خواهی این کار را بکنی با اینکه سایت‌ها جلوی ارتباط را می‌گیرند؟ آیا اصلاً می‌شود؟»

- **بله می‌شود.** شواهد قوی (پروژه‌های فعال، پکیج PyPI، سرویس‌های تجاری) نشان می‌دهد API ترب بدون کلید کار می‌کند.
- **خطای فعلی ما مربوط به محدودیت شبکه‌ی سندباکس است**، نه بلاک شدن توسط ترب.
- راهکار فنی: **افزونه‌ی وردپرس با WordPress HTTP API**، هدرهای مرورگر، **چند لایه fallback** (مستقیم/HTML/شخص‌ثالث)، کش، تاخیر و صفحه‌ی تنظیمات با دکمه‌ی تست اتصال.
- **پیش‌نیاز اجرایی:** افزونه باید روی سرور/هاستی نصب شود که به `torob.com` دسترسی داشته باشد (در عمل هاست ایران). در صفحه‌ی تنظیمات این مورد به‌صورت خودکار بررسی و به کاربر گزارش می‌شود.
- توسعه در این محیط با **داده‌ی نمونه (mock)** انجام می‌شود و تست نهایی اتصال روی هاست واقعی شما صورت می‌گیرد.

اگر با این رویکرد موافقید، در گام بعد:
1. یک **اسکریپت تست اتصال ساده‌ی PHP** می‌سازم تا روی هاست واقعی امتحان کنید و خروجی را ببینیم، و
2. همزمان اسکلت افزونه را با داده‌ی نمونه شروع می‌کنم.
