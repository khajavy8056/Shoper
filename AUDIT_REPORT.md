# گزارش بررسی امنیتی و تست افزونه Shoper (نسخه ۱.۲.۰)

> تاریخ: ۲۰۲۶-۰۸-۱۸
> شاخه: `arena/01a01165-shoper`
> وضعیت: ✅ تأییدشده — ۴۵ تست رابط کاربری پاس، آرشیو ZIP آماده‌ی نصب

---

## ۱. خلاصه

افزونه‌ی Shoper (درون‌ریز محصول از ترب به ووکامرس) نسخه‌ی ۱.۲.۰ از نظر
**امنیتی** و **عملکردی** بررسی شد. هیچ باگ امنیتیِ بحرانی یا شناخته‌شده‌ای در
این نسخه باقی نمانده است. یک نقطه‌ی XSS بالقوه (پیام‌های کنترل‌نشده از API در
`status()`) شناسایی و اصلاح شد.

---

## ۲. بررسی امنیتی

### ۲.۱ جلوگیری از XSS (اسکریپت‌های تزریق‌شده)

تمام داده‌هایی که از **ترب** می‌آیند (نام، توضیحات، مشخصات، عنوان فروشنده) و در
رابط کاربری پنل مدیریت وردپرس رندر می‌شوند، بررسی شدند:

| مسیر رندر | وضعیت | توضیح |
|---|---|---|
| نتایج جستجو / پیشنهاد کشویی (`renderResults`, `renderSuggest`) | ✅ ایمن | همه از `this.esc()` / `this.highlight()` (اول escape سپس `<mark>`) عبور می‌کنند |
| پیش‌نمایش محصول (`renderPreview`) | ✅ ایمن | نام، توضیحات، گالری، فروشندگان همگی `esc()` شده‌اند |
| پیام‌های وضعیت (`status()`) | ✅ اصلاح شد | قبل از این نسخه پیام‌های برگشتی از سرور/API بدون escape رندر می‌شدند؛ حالا `this.esc(msg)` می‌شوند |
| توضیحات HTML در سمت PHP | ✅ ایمن | `esc_html()` و `wp_kses_post()` |
| جدول مشخصات و فروشندگان (PHP) | ✅ ایمن | `esc_html()` / `esc_attr()` |

`esc()` در admin.js از `$('<div>').text(...).html()` استفاده می‌کند که همه‌ی
نویسه‌های HTML را رمزگذاری می‌کند — در برابر XSS امن است.

### ۲.۲ بررسی دسترسی (Authorization)

همه‌ی اکشن‌های AJAX (`shoper_suggest`، `shoper_search`، `shoper_preview`،
`shoper_create`، `shoper_fill`، `shoper_test_connection`) با متد `guard()`
محافظت می‌شوند که شامل:
- `check_ajax_referer('shoper_nonce', 'nonce')` — بررسی nonce (ضد CSRF).
- `current_user_can('edit_products')` — فقط کاربر با مجوز ویرایش محصول.

فرم تنظیمات هم با `check_admin_referer('shoper_settings')` محافظت می‌شود.

### ۲.۳ SSRF / درخواست به آدرس دلخواه

- `details_from_url()` فقط `prk` را از URL کاربر استخراج می‌کند و آن را به
  دامنه‌ی ثابت `api.torob.com` می‌دهد — هیچ درخواستی به آدرس دلخواه کاربر نمی‌رود.
- آدرس تصاویر هم فقط به `api.torob.com`/`image.torob.com` تعلق دارد و با
  `media_handle_sideload` دانلود می‌شود که نوع فایل را اعتبارسنجی می‌کند.

### ۲.۴ SQL Injection

همه‌ی کوئری‌های مستقیم دیتابیس با `$wpdb->prepare()` ساخته می‌شوند
(`find_existing`, `find_by_source_url`). جستجوهای ترب از طریق `add_query_arg`
انجام می‌شوند که URL-encode می‌شود.

### ۲.۵ Sanitization ورودی

همه‌ی ورودی‌های `$_POST` با `sanitize_text_field`، `wp_kses_post`، `esc_url_raw`،
`absint` پاکسازی می‌شوند. نام فایل تصویر با `sanitize_file_name` و اسلاگ ویژگی
با `sanitize_title` + نقشه‌ی دستی پاکسازی می‌شود.

### ۲.۶ ذخیره‌ی امن داده

- تصاویر در **کتابخانه‌ی رسانه** (`media_handle_sideload`) ذخیره می‌شوند — نه
  لینک هات‌لینک به سرور خارجی.
- کلید API یا توکن‌ی در افزونه ذخیره نمی‌شود.
- متاهای مبدأ (`_shoper_*`) با توابع استاندارد وردپرس ثبت می‌شوند.

---

## ۳. بررسی عملکردی (فلوهای کاربر)

### ۳.۱ جستجو با نام ناقص → نوار کشویی پیشنهاد نام کامل
- تایپ «سام» → ۵ پیشنهاد نام کامل با تصویر، قیمت و تعداد فروشگاه. ✅
- ناوبری با ↑ ↓ و انتخاب با Enter. ✅
- **فروشندگان تبلیغاتی (`is_adv`) از پیشنهادها حذف می‌شوند.** ✅

### ۳.۲ ورود با لینک مستقیم
- استخراج `prk` از لینک `/p/<uuid>/...` و رفتن مستقیم به پیش‌نمایش. ✅

### ۳.۳ نوار مراحل سه‌گانه + نوار پیشرفت
- ۱) دریافت اطلاعات → ۲) انتخاب تصاویر → ۳) سئو و برچسب. ✅
- نوار پیشرفت هنگام ساخت محصول فعال می‌شود. ✅

### ۳.۴ مشخصات فنی به‌صورت ویژگی‌های مجزا
- ۴۰ مشخصه در خروجی؛ هر کدام یک `WC_Product_Attribute` مجزا. ✅
- اگر ویژگی (مثل «پردازنده»، «رنگ»، «نوع شنا») وجود نداشته باشد، خودکار
  ساخته می‌شود (taxonomy سراسری با slug لاتین). ✅
- **رنگ/رنگ‌بندی نیز به‌عنوان ویژگی ثبت می‌شود.** ✅

### ۳.۵ انتخاب تصاویر + تصویر اصلی + نام فایل
- تیک «نگه‌داشته شود» برای هر تصویر + دکمه‌ی «تصویر اصلی». ✅
- تصاویر با نام `{نام محصول}-{شماره}` در کتابخانه‌ی رسانه ذخیره می‌شوند. ✅

### ۳.۶ سئو و برچسب
- عنوان سئو، توضیح متا و برچسب‌ها خودکار ساخته و قابل ویرایش‌اند. ✅
- برچسب‌ها در `product_tag` و متادیتای سئو (و Yoast در صورت نصب) ثبت می‌شوند. ✅

### ۳.۷ بررسی چند فروشنده (نه همه)
- فقط ۳ فروشنده‌ی برتر بررسی می‌شوند (قابل تنظیم). ✅
- معیار پیش‌فرض: بالاترین امتیاز/اعتبار فروشگاه، نه ارزان‌ترین. ✅

### ۳.۸ دکمه‌ی اختصاصی در صفحه‌ی افزودن/ویرایش محصول (متاباکس)
- متاباکس از همان جریان سه‌مرحله‌ای (استپر، گرید تصویر، سئو) استفاده می‌کند. ✅

---

## ۴. تنظیمات در دسترس از پنل

| گزینه | پیش‌فرض |
|---|---|
| `shoper_seller_limit` | ۳ |
| `shoper_seller_strategy` | score (معتبرترین) |
| `shoper_product_status` | draft |
| `shoper_import_gallery` | yes |
| `shoper_price_behavior` | cheapest |

---

## ۵. نتایج تست

### تست خودکار رابط کاربری (jsdom — اجرای فایل‌های واقعی افزونه)
```
نتیجه: ✅ 45 موفق  ❌ 0 ناموفق
```

### آرشیو قابل‌نصب
```
build/shoper-torob-importer-1.2.0.zip  (53 KB)
```
محتوا: پوشه‌ی `shoper-torob-importer/` در ریشه (برای `wp-content/plugins/`).

---

## ۶. محدودیت‌های محیط بررسی

- این محیط سندباکس به اینترنت عمومی دسترسی ندارد؛ بنابراین تست **زنده‌ی
  اتصال به api.torob.com** در اینجا انجام نشده، اما در جلسات قبل با ابزار
  `fetch_page` اتصال واقعی تأیید شده است (مستند در `LIVE_TEST_RESULTS.md`).
- PHP و MySQL در این محیط قابل نصب نیستند، بنابراین تست لایه‌ی PHP با شبیه‌ساز
  منطق (`preview/server/torob-logic.js`) و تست رابط با jsdom انجام شد. منطق
  PHP و JS به‌صورت آینه‌ای هم‌سو نگه داشته می‌شوند.
- API ترب نیاز به کلید ندارد و یک `GET` ساده است؛ روی هاست ایران کار می‌کند.
