# بررسی دقیق افزونه‌ی مشابه: Digikala Product Scraper

> مخزن: [alireza-aminzadeh/digikala-woocommerce-importer](https://github.com/alireza-aminzadeh/digikala-woocommerce-importer)
> نسخه: ۱.۶.۰ | نویسنده: Alireza Aminzadeh | لایسنس: GPL v2
> بررسی در تاریخ: ۲۰۲۶-۰۸-۱۵

---

## ۱. این افزونه دقیقاً چیست و چطور نصب می‌شود؟

این یک **افزونه‌ی استاندارد وردپرس** است (یک پوشه‌ی PHP که در مسیر `wp-content/plugins/` کپی می‌شود).

### ساختار فایل‌ها

```
digikala-woocommerce-importer/
├── digikala-product-scraper.php     ← فایل اصلی (header افزونه + کلاس اصلی + AJAX handlers)
├── includes/
│   ├── class-digikala-scraper.php   ← منطق اسکرپ (دانلود HTML و پارس کردن)
│   └── class-woocommerce-importer.php ← منطق ساخت محصول در ووکامرس
└── admin/
    ├── admin-page.php               ← صفحه‌ی مدیریت و رندر تب‌ها
    ├── dashboard.php                ← داشبورد آمار
    ├── scrape.php                   ← فرم وارد کردن URL و شروع اسکرپ
    ├── history.php                  ← تاریخچه‌ی importها
    ├── settings.php                 ← تنظیمات (تاخیر، User-Agent، import تصویر و...)
    ├── css/ و js/                   ← استایل و اسکریپت پنل ادمین
```

### نصب

1. پوشه‌ی افزونه در `wp-content/plugins/` کپی می‌شود.
2. از مسیر **افزونه‌ها** در پیشخوان وردپرس فعال می‌شود.
3. در فعال‌سازی، یک جدول دیتابیس به نام `{prefix}_dps_scraping_logs` ساخته می‌شود (برای لاگ‌ها).
4. در منوی پیشخوان یک آیتم جدید اضافه می‌شود.
5. **پیش‌نیاز:** اگر ووکامرس فعال نباشد، افزونه پیام خطا می‌دهد و کار نمی‌کند:
   ```php
   if (!in_array('woocommerce/woocommerce.php', apply_filters('active_plugins', get_option('active_plugins')))) {
       add_action('admin_notices', ...);  // خطا
       return;
   }
   ```

---

## ۲. چطور کار می‌کند؟ (جریان اجرا)

این افزونه بر پایه‌ی **HTML scraping با DOMDocument و XPath** کار می‌کند — نه API ج‌سان.

### الف) تعامل با UI (AJAX)

کلاس اصلی این handlerهای AJAX وردپرس را ثبت می‌کند:

| اکشن AJAX | کار |
|-----------|-----|
| `dps_scrape_digikala` | شروع اسکرپ (URL دسته، تعداد صفحه، تاخیر) |
| `dps_get_scraping_progress` | گرفتن پیشرفت کار (با transient) |
| `dps_test_connection` | تست دسترسی به URL |
| `dps_save_settings` | ذخیره‌ی تنظیمات |
| `dps_reset_statistics` / `dps_cleanup_logs` / `dps_bulk_action` | مدیریت لاگ‌ها |

هر درخواست با `check_ajax_referer('dps_nonce')` محافظت می‌شود و فقط کاربر با دسترسی `manage_options` اجازه دارد.

### ب) منطق اسکرپ (`class-digikala-scraper.php`)

1. **ورودی:** آدرس یک صفحه‌ی لیست محصولات دیجی‌کالا (مثلاً صفحه‌ی دسته یا جستجو) و تعداد صفحات.
2. برای هر صفحه:
   - HTML با `wp_remote_get` دانلود می‌شود (با `User-Agent` قابل تنظیم).
   - با `DOMDocument` + `DOMXPath` پارس می‌شود.
   - با چند selector سعی می‌کند محصولات را پیدا کند:
     ```
     //div[contains(@class, "c-product-box")]
     //div[contains(@class, "product-item")]
     ...
     ```
   - برای هر کارت محصول، این فیلدها استخراج می‌شود:
     - **عنوان** (`h3` با کلاس‌های `c-product-box__title` و ...)
     - **قیمت** و **قیمت قبل از تخفیف**
     - **تصویر** (تگ `img`)
     - **لینک محصول**
     - **SKU / شناسه** (از `data-product-id`)
     - **برند**
     - **امتیاز و تعداد نظرات**
3. بین صفحات `sleep($delay)` می‌کند (پیش‌فرض ۳ ثانیه) تا بلاک نشود.
4. پیشرفت کار در یک transient به نام `dps_scraping_progress` ذخیره و با polling از فرانت‌اند نمایش داده می‌شود.

> ⚠️ **نکته‌ی مهم:** این افزونه صفحه‌ی **لیست/دسته** را اسکرپ می‌کند، نه صفحه‌ی تکی هر محصول. در نتیجه **مشخصات فنی (ویژگی‌ها/attributes) را اصلاً استخراج نمی‌کند** — فقط عنوان، قیمت، تصویر، برند و امتیاز از کارت‌های لیست.

### ج) منطق وارد کردن به ووکامرس (`class-woocommerce-importer.php`)

برای هر محصول:

1. **بررسی تکراری بودن با SKU:**
   ```php
   $existing_product_id = wc_get_product_id_by_sku($product_data['sku']);
   ```
   اگر SKU وجود داشت → محصول به‌روزرسانی می‌شود؛ وگرنه جدید ساخته می‌شود.

2. **ساخت محصول ساده:**
   ```php
   $product = new WC_Product_Simple();
   $product->set_name($title);
   $product->set_status('publish');
   $product->set_description(...);
   $product->set_short_description(...);
   $product->set_regular_price($price);
   $product->set_sku($sku);
   $product->save();
   ```

3. **افزودن ویژگی برند (فقط برند!):**
   ```php
   $attribute = new WC_Product_Attribute();
   $attribute->set_name('brand');
   $attribute->set_options(array($brand));
   $attribute->set_position(0);
   $attribute->set_visible(true);
   $attribute->set_variation(false);
   $attributes[] = $attribute;
   $product->set_attributes($attributes);
   ```
   این یک ویژگی **محلی (local)** است — نه taxonomy سراسری `pa_brand`. یعنی برند در صفحه‌ی محصول نمایش داده می‌شود اما در ویجت فیلتر فروشگاه ظاهر نمی‌شود.

4. **دانلود تصویر شاخص (Featured Image):**
   ```php
   $upload = media_sideload_image($image_url, $product_id, '', 'id');
   set_post_thumbnail($product_id, $upload);
   ```
   قبل از دانلود، با کوئری مستقیم دیتابیس بررسی می‌کند که آیا قبلاً این URL به‌عنوان attachment وجود دارد یا نه.

5. **ذخیره‌ی متا:**
   - `_digikala_url` → لینک مبدأ
   - `_digikala_imported` → زمان import

6. **تولید توضیحات:** یک توضیح HTML ساده با عنوان و برند می‌سازد (نه متن واقعی محصول).

---

## ۳. نقاط قوت این افزونه

- ساختار تمیز و استاندارد افزونه‌ی وردپرس (کلاس‌محور، فایل‌بندی منطقی).
- داشتن صفحه‌ی تنظیمات، داشبورد آمار و تاریخچه‌ی import.
- AJAX با nonce و بررسی دسترسی کاربر.
- جلوگیری از ساخت محصول تکراری با SKU.
- دانلود تصویر در Media Library با `media_sideload_image`.
- پشتیبانی از به‌روزرسانی محصول موجود.
- progress tracking با transient.
- تنظیمات import تصویر/توضیحات/ویژگی به‌صورت سوییچ.

---

## ۴. نقاط ضعف و تفاوت با نیاز ما

| موضوع | وضعیت در افزونه‌ی دیجی‌کالا | آنچه ما برای ترب لازم داریم |
|-------|---------------------------|---------------------------|
| روش دریافت داده | HTML scraping با XPath (شکننده با تغییر قالب سایت) | **API ج‌سان ساختاریافته‌ی ترب** (پایدارتر و داده‌ی غنی‌تر) |
| ورودی کاربر | فقط URL صفحه‌ی لیست/دسته | **نام محصول (اولویت اول)** + لینک تکی محصول |
| پیش‌نمایش قبل از ساخت | ❌ ندارد — مستقیم `publish` می‌کند | ✅ باید باشد (خواسته‌ی صریح شما) |
| مشخصات فنی | ❌ اصلاً استخراج نمی‌شود | ✅ همه‌ی `structural_specs` یکی‌یکی به‌عنوان attribute |
| ویژگی‌ها | فقط `brand` به‌صورت local | ده‌ها ویژگی (پردازنده، وزن، نوع شنا...)؛ ترجیحاً taxonomy برای مهم‌ها |
| گالری تصاویر | ❌ فقط یک تصویر شاخص | ✅ تصویر اصلی + گالری کامل |
| توضیحات محصول | متن تولیدی کلی | متن/توضیح واقعی + جدول مشخصات |
| نوع محصول | فقط simple | simple در فاز اول، بعد variable با variants |
| وضعیت انتشار | مستقیم publish | پیشنهاد draft برای بازبینی |
| مقیاس | اسکرپ لیست (انبوه) | جستجو/ساخت یک محصول در هر بار (با دقت بالا) |
| فارسی‌سازی (RTL) | ظاهراً انگلیسی | رابط کاربری کاملاً فارسی/RTL |

---

## ۵. درس‌هایی که برای افزونه‌ی Shoper می‌گیریم

### چیزهایی که از این الگو وام می‌گیریم:
- همان ساختار پوشه‌ای استاندارد: `shoper-torob-importer.php` + `includes/` + `admin/`.
- لایه‌بندی سه‌گانه: **Client (دریافت داده)** ← **Mapper (تبدیل به مدل محصول)** ← **Builder (ساخت در ووکامرس)**.
- AJAX handlers با nonce در کلاس اصلی.
- صفحه‌ی تنظیمات، داشبورد و تاریخچه‌ی import.
- بررسی تکراری بودن با SKU (ما از `random_key` ترب استفاده می‌کنیم، مثلاً `TRB-{random_key}`).
- دانلود تصاویر با `media_sideload_image`.
- ذخیره‌ی لینک مبدأ در متا (`_torob_url` و `_torob_random_key`).
- transient برای پیشرفت کار.

### چیزهایی که بهتر/متفاوت می‌سازیم:
- به‌جای scraping، از API ج‌سان ترب استفاده می‌کنیم (با fallback به سرویس شخص‌ثالث).
- جستجو با **نام محصول** به‌عنوان ورودی اصلی.
- یک **صفحه‌ی پیش‌نمایش** واقعی قبل از ساخت (با امکان ویرایش عنوان، قیمت، توضیحات و انتخاب ویژگی‌ها).
- import **گالری کامل تصاویر** نه فقط یک عکس.
- افزودن **همه‌ی مشخصات فنی** به‌صورت attribute (با مدیریت slug فارسی→لاتین).
- وضعیت پیش‌فرض `draft`.
- رابط کاربری کاملاً فارسی و راست‌چین.
- لایه‌ی انتزاعی برای منبع داده تا اگر API ترب تغییر کرد، سوییچ ممکن باشد.

---

## ۶. جمع‌بندی

این افزونه یک **نمونه‌ی مرجع خوب برای ساختار و مهندسی افزونه** است، اما از نظر **داده و قابلیت** بسیار سطحی‌تر از چیزی است که شما می‌خواهید:
- عمداً روی **اسکرپ انبوه صفحات لیست** تمرکز کرده.
- هیچ پشتیبانی‌ای از **ویژگی‌های فنی محصول** ندارد (فقط برند).
- پیش‌نمایش و کنترل قبل از ساخت ندارد.

برای Shoper ما همان اسکلت استاندارد را نگه می‌داریم اما موتور آن را با API ترب، استخراج کامل `structural_specs`، گالری تصاویر و صفحه‌ی پیش‌نمایش بازطراحی می‌کنیم.
